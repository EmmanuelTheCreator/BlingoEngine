using AbstUI.Commands;

namespace LingoEngine.Director.Core.Remote.Commands;

/// <summary>Connects the Director RNet client to the host.</summary>
public sealed record ConnectRNetClientCommand() : IAbstCommand;

